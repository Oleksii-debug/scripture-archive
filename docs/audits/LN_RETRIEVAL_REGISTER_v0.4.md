# LN retrieval register v0.4

**Campaign:** LN — «Остання ніч»  
**Date:** 2026-08-19  
**Status:** `IN_PROGRESS — MARK ROOSTER TX1 CHAIN EXACT-ID CLOSED`  
**Supersedes:** v0.3 for current audit work; older versions remain history.

## Exact links closed/retained
- `LN04-N01 → LN09-N02` — CROSS_CONTEXT, `RESOLVED_NODE`, prediction corpus → fulfilment corpus.
- `LN04-N02 → LN09-N01` — CROSS_CONTEXT delayed recall, `RESOLVED_NODE`.
- `LN04-N09 → LN09-N11` — VARIANT/CROSS_CONTEXT TX1 policy application, `RESOLVED_NODE`.
- `LN09-N11 → LN12-N08` — CROSS_CONTEXT prediction→fulfilment synthesis with TX1 retained, `RESOLVED_NODE`.
- `LN09-N13 → LN12-N08` — SYNTHESIS carry-forward, `RESOLVED_NODE`.
- `LN09-N14 → LN12-N08` — final denial-case synthesis carry-forward, `RESOLVED_NODE`.
- `LN09-O01 → LN12-N10` — optional textual-variant deep dive → final campaign variant audit, `RESOLVED_NODE`.
- prior LN07→LN08 exact daybreak/provenance links remain unchanged.

## Mark rooster TX1 invariant
Primary chain is now:
`LN04-N09 (prediction wording/TX1) → LN09-N11 (fulfilment wording/TX1) → LN12-N08 (prediction→fulfilment synthesis)`.
Final audit safety net: `LN09-O01 → LN12-N10`.

At every hop:
- confidence code remains T1/T2 as appropriate; TX1 is an adjunct flag;
- variant information is player-visible before grading;
- Mark 14:68 first-crow clause is treated as materially textually variable and no originality verdict is required;
- responsible translations/textual forms are accepted;
- retrieval never converts the Mark-specific twice/second-crow form into a universal four-Gospel claim.

## Closure effect
The high-risk Mark chain that blocked the R01 target is exact-ID closed. **D-004 remains HIGH/OPEN campaign-wide** because LN-01/02/03/05/06/10/11/12 still contain retrieval hooks awaiting full v1.2 normalization and classification.

## Next D-004 work
1. normalize LN-11/LN-12;
2. normalize LN-10 and close daybreak destination from LN08-N10;
3. scan remaining missions and classify every hook;
4. run final no-UNRESOLVED assertion;
5. rerun player histories against the complete register.
